[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "am"
version = "1.0.0"
description = "Command-line client for market.near.ai"
requires-python = ">=3.9"
dependencies = ["click>=8.0", "requests>=2.31"]

[project.scripts]
am = "cli:cli"
